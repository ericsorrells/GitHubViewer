'use strict';

describe('myApp.repos module', function() {

  beforeEach(module('myApp.repos'));

  describe('repos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reposCtrl = $controller('reposCtrl');
      expect(reposCtrl).toBeDefined();
    }));

  });
});